<div class='user-card-small'>
  <% if user.image.attached? %>
  <%= cl_image_tag(user.image.key, class:'avatar') %>
  <% else %>
  <%= image_tag('default-avatar', class:'avatar') %>
  <% end %>
  <%= user.full_name %>
</div>