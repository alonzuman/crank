<%= render  "shared/top-bar", center:'Crank' %>
<div class="container">
  <% @posts.each do |post| %>
  <%= render  "shared/post-card", post: post %>
  <% end %>
</div>

<%= render 'shared/navbar' %>
</div>
