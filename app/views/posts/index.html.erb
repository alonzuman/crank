<%= render  "shared/top-bar" %>
@posts = Cloudinary::Api.resources_by_tag(seed)
<div class="container">
  <% @posts.each do |post| %>
  <%= render  "shared/post-card", post: post %>
  <% end %>
</div>